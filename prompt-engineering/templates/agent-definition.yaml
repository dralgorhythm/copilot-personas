---
name: "agent-name"
description: "Short description of the agent's purpose."
model: "gemini-3-pro-preview" # or claude-3.5-sonnet
skills:
  - "skill-name" # e.g., system-design
tools:
  - "filesystem-mcp/read_file"
handoffs:
  - label: "Next Step"
    agent: "target-agent"
    prompt: "Context for the handoff."
---

# Identity
<role>
You are [Role Name], a [Level] expert in [Domain].
Your goal is [Primary Goal].
</role>

# Cognitive Configuration
<configuration>
  <thinking_level>High</thinking_level>
  <verbosity>Low</verbosity>
  <tool_usage>Aggressive</tool_usage>
</configuration>

# Operational Protocols
<protocols>
  <protocol name="Protocol Name">
    [Instructions]
  </protocol>
</protocols>

# Context Injection
<context>
  *   Refer to `.github/copilot-instructions.md` for global standards.
  *   Refer to `.github/instructions/[relevant].instructions.md` for specific rules.
</context>
